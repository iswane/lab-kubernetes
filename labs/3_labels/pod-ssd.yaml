apiVersion: v1
kind: Pod
metadata:
  name: pod-ssd
  namespace: lab-aug-2026
  labels:
    app: backend
    release: beta
spec:
  nodeSelector:
    disk: ssd
  containers:
  - name: nginx-ssd
    image: nginx
    ports:
      - containerPort: 80
